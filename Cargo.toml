[package]
name = "si-variational-agent"
version = "0.1.0"
edition = "2021"
description = "Variational principle for optimal agent budget allocation — minimize action functional subject to conservation constraint"
license = "MIT"
